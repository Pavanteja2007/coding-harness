"""Terminal 4's read-only dashboard (spec item 40, stretch/Phase 6 polish).

A thin visualization layer over the structured logs the system already
writes — no new backend, no database, no write path anywhere.
"""
