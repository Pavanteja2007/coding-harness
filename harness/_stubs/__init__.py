"""Local stubs for other terminals' modules (INTERFACES.md contracts).

These are NOT the real implementations — they exist so Terminal 1 can build
and test the harness before Terminals 2 and 3 land. Signatures match the
contracts exactly. harness/deps.py prefers the real modules when importable.
"""
