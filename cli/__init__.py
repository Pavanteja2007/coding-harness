"""Terminal 4 — CLI: the human-facing interface tying the system together.

Entry point: cli.main (console script ``harness``, or ``python -m cli``).
Commands (INTERFACES.md Boundary 6): fix, run-benchmark, status.
"""
